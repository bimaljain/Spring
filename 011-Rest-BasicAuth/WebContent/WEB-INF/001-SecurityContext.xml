<beans:beans xmlns="http://www.springframework.org/schema/security"
	xmlns:beans="http://www.springframework.org/schema/beans" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans-4.1.xsd
    http://www.springframework.org/schema/security http://www.springframework.org/schema/security/spring-security-4.0.xsd">
	
	<!-- create-session="stateless" : This is needed otherwise the session will store the credentials. So when you try to use the API with credentials,
	you will still be able to access API -->
	
<!-- 	<http authentication-manager-ref="in-memory-auth" auto-config="true" create-session="stateless"> -->
<!-- 	<http authentication-manager-ref="jdbc-auth" auto-config="true" create-session="stateless"> -->
	<http authentication-manager-ref="dao-auth" auto-config="true" create-session="stateless">
		<intercept-url pattern="/001/user/" access="hasRole('ADMIN')" />
		<http-basic />
	</http>
	
	<authentication-manager id="in-memory-auth">
		<authentication-provider>
			<user-service>
				<user name="bimal" password="bimal" authorities="ROLE_USER" />
				<user name="admin" password="admin" authorities="ROLE_ADMIN" />
				<user name="db" password="db" authorities="ROLE_DBA" />
			</user-service>
		</authentication-provider>
	</authentication-manager>	
	
	<authentication-manager id="jdbc-auth">
		<authentication-provider>
		 <!-- query must return 3 columns. Third column is ENABLED. We have returned just 1 -->
			<jdbc-user-service data-source-ref="dataSource"
				users-by-username-query="select user_id,password,1 from user_details where user_id = ?"
				authorities-by-username-query="select user_id,role from user_role where user_id = ?" />
		</authentication-provider>
	</authentication-manager>
	
	<authentication-manager id="dao-auth">
		<authentication-provider user-service-ref="userDetailsService" />
	</authentication-manager>
	
	<beans:bean id="userDetailsService" class="_001.UserDetailsServiceDAO" />

</beans:beans>